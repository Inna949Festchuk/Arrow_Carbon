# Arrow_Carbon

Проект для расчета предельно допустимой рекреационной емкости (ПДРЕ) и углеродных единиц на основе спутниковых снимков и геоданных.

## 📋 Содержание

- [Описание проекта](#описание-проекта)
- [Основные возможности](#основные-возможности)
- [Технологический стек](#технологический-стек)
- [Быстрый старт](#быстрый-старт)
- [Структура проекта](#структура-проекта)
- [Документация](#документация)
- [Разработка](#разработка)
- [API](#api)
- [Устранение неисправностей](#устранение-неисправностей)

## Описание проекта

Arrow_Carbon — это веб-приложение на базе Django и GeoDjango для расчета предельно допустимой рекреационной емкости (ПДРЕ) особо охраняемых природных территорий (ООПТ). Проект реализует методику расчета согласно Постановлению №1809 и позволяет:

- Импортировать данные ООПТ из OpenStreetMap
- Рассчитывать ПДРЕ для площадных и линейных туристских объектов
- Визуализировать результаты на интерактивных картах
- Генерировать паспорта территорий с результатами расчетов
- Работать с растровыми и векторными геоданными

## Основные возможности

### 🗺️ Управление ООПТ
- Создание и редактирование особо охраняемых природных территорий
- Импорт данных из OpenStreetMap
- Автоматический расчет площади территорий
- Хранение границ в формате GeoJSON/Polygon

### 📊 Расчет ПДРЕ
- Расчет базовой емкости (BCC) для площадных и линейных объектов
- Расчет потенциальной емкости (PCC) с учетом поправочных коэффициентов
- Расчет предельно допустимой емкости (RCC) с учетом управленческого фактора
- Поддержка различных типов туризма (однодневный, многодневный, автономный)

### 🎯 Туристские объекты
- Управление площадными, линейными и точечными объектами
- Импорт маршрутов из GPX-файлов
- Автоматический расчет параметров объектов
- Привязка объектов к ООПТ

### 📈 Визуализация и отчетность
- Генерация интерактивных карт с результатами расчетов
- История расчетов ПДРЕ
- Паспорта территорий с рекомендациями
- Экспорт данных в различных форматах

### ⚙️ Асинхронная обработка
- Фоновые задачи через Celery
- Импорт больших объемов данных
- Расчет параметров территории
- Генерация отчетов

## Технологический стек

### Backend
- **Django 4.2+** — веб-фреймворк
- **GeoDjango** — работа с пространственными данными
- **Django REST Framework** — REST API
- **PostgreSQL + PostGIS** — база данных с поддержкой геоданных
- **Celery + Redis** — асинхронные задачи
- **Django Celery Beat** — планировщик задач

### Гео-библиотеки
- **GeoPandas** — работа с векторными данными
- **OSMnx** — импорт данных из OpenStreetMap
- **Rasterio** — работа с растровыми данными
- **Shapely** — геометрические операции
- **Folium** — визуализация карт
- **PyProj** — проекции координат

### Инфраструктура
- **Docker** — контейнеризация
- **Docker Compose** — оркестрация сервисов
- **Nginx** (опционально) — веб-сервер

## Быстрый старт

### Предварительные требования

- Docker и Docker Compose установлены
- Минимум 4 ГБ свободной оперативной памяти
- 10 ГБ свободного места на диске

Проверьте установку:
```bash
docker --version
docker compose version
```

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd Arrow_Carbon
```

### 2. Настройка окружения

Создайте файл `.env` в корне проекта:

```bash
cat > .env << 'EOF'
POSTGRES_DB=pdre_db
POSTGRES_USER=pdre_user
POSTGRES_PASSWORD=pdre_password

# Секретный ключ Django (обязательно измените в продакшене!)
DJANGO_SECRET_KEY=dev-secret-key-change-me

# Режим отладки (1 для разработки, 0 для продакшена)
DJANGO_DEBUG=1

# Разрешенные хосты (через запятую)
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
EOF
```

### 3. Запуск проекта

```bash
# Сборка образов
docker compose build

# Запуск всех сервисов
docker compose up
```

При первом запуске Django автоматически выполнит миграции базы данных.

### 4. Создание суперпользователя

```bash
docker compose exec web python manage.py createsuperuser
```

Следуйте инструкциям для создания администратора.

### 5. Доступ к приложению

- **Веб-приложение**: http://localhost:8000/
- **Админ-панель**: http://localhost:8000/admin/
- **API**: http://localhost:8000/api/pdre/
- **PostgreSQL**: localhost:5432
- **Redis**: localhost:6379

## Структура проекта

```
Arrow_Carbon/
├── manage.py                 # Точка входа Django
├── docker-compose.yml        # Конфигурация Docker Compose
├── Dockerfile               # Образ для Django-приложения
├── requirements.txt         # Python-зависимости
├── .env                     # Переменные окружения (создается вручную)
│
├── web_gis_project/         # Основной проект Django
│   ├── settings.py          # Настройки проекта
│   ├── urls.py              # Главный URL-роутер
│   ├── wsgi.py              # WSGI-конфигурация
│   └── celery.py            # Конфигурация Celery
│
├── pdre_calculation/        # Приложение для расчета ПДРЕ
│   ├── models.py            # Модели данных
│   ├── views.py             # API-представления
│   ├── serializers.py       # Сериализаторы DRF
│   ├── urls.py              # URL-маршруты API
│   ├── services.py          # Бизнес-логика (импорт, визуализация)
│   ├── calculation_methods.py # Методика расчета ПДРЕ
│   ├── tasks.py             # Celery-задачи
│   └── admin.py             # Админ-панель
│
├── carbon_tracker/          # Приложение для отслеживания углерода (заготовка)
├── fleet/                   # Приложение для управления флотом (заготовка)
│
├── media/                   # Загруженные файлы
├── static/                  # Статические файлы
└── init-postgis-raster.sql  # SQL-скрипт для PostGIS Raster
```

## Документация

Подробная документация доступна в следующих файлах:

- **[API.md](docs/API.md)** — описание REST API
- **[ARCHITECTURE.md](docs/ARCHITECTURE.md)** — архитектура проекта
- **[MODELS.md](docs/MODELS.md)** — описание моделей данных
- **[DEVELOPMENT.md](docs/DEVELOPMENT.md)** — руководство по разработке

## Разработка

### Локальная разработка без Docker

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Установите системные библиотеки для GeoDjango:
```bash
# Ubuntu/Debian
sudo apt-get install gdal-bin libgdal-dev libgeos-dev libproj-dev libpq-dev

# macOS
brew install gdal geos proj postgresql
```

3. Настройте базу данных PostgreSQL с PostGIS

4. Выполните миграции:
```bash
python manage.py migrate
```

5. Запустите сервер разработки:
```bash
python manage.py runserver
```

### Полезные команды

```bash
# Просмотр логов
docker compose logs -f web
docker compose logs -f celery_worker

# Выполнение команд Django
docker compose exec web python manage.py <command>

# Создание миграций
docker compose exec web python manage.py makemigrations

# Применение миграций
docker compose exec web python manage.py migrate

# Создание суперпользователя
docker compose exec web python manage.py createsuperuser

# Запуск shell Django
docker compose exec web python manage.py shell

# Остановка контейнеров
docker compose down

# Полная очистка (включая volumes)
docker compose down -v
```

## API

Проект предоставляет REST API для работы с данными. Основные эндпоинты:

- `GET /api/pdre/protected-areas/` — список ООПТ
- `POST /api/pdre/protected-areas/` — создание ООПТ
- `POST /api/pdre/protected-areas/{id}/calculate_pdre/` — запуск расчета ПДРЕ
- `GET /api/pdre/tourism-objects/` — список туристских объектов
- `GET /api/pdre/calculation-results/` — результаты расчетов

Подробная документация API доступна в [API.md](docs/API.md).

## Устранение неисправностей

### Проблемы с миграциями

1. Проверьте наличие файла models.py:
```bash
docker compose exec web ls -la pdre_calculation/models.py
```

2. Создайте миграции:
```bash
docker compose exec web python manage.py makemigrations pdre_calculation
```

3. Примените миграции:
```bash
docker compose exec web python manage.py migrate
```

4. Проверьте статус миграций:
```bash
docker compose exec web python manage.py showmigrations pdre_calculation
```

### Проблемы с PostGIS Raster

Если возникают ошибки при работе с растровыми данными:

1. Подключитесь к базе данных:
```bash
docker compose exec db psql -U pdre_user -d pdre_db
```

2. Установите расширение:
```sql
CREATE EXTENSION IF NOT EXISTS postgis_raster;
\q
```

### Проблемы с GDAL/GEOS

Если возникают ошибки с гео-библиотеками:

1. Проверьте пути к библиотекам в `settings.py`
2. В Docker-контейнере пути должны быть:
   - `GDAL_LIBRARY_PATH=/usr/lib/x86_64-linux-gnu/libgdal.so.32`
   - `GEOS_LIBRARY_PATH=/usr/lib/x86_64-linux-gnu/libgeos_c.so.1`

### Проблемы с Celery

1. Проверьте, что Redis запущен:
```bash
docker compose ps redis
```

2. Проверьте логи Celery:
```bash
docker compose logs -f celery_worker
```

3. Перезапустите воркер:
```bash
docker compose restart celery_worker
```

## Лицензия

См. файл [LICENSE](LICENSE) для подробной информации.

## Контакты

Для вопросов и предложений создайте issue в репозитории проекта.
